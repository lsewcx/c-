string a
getline(cin,a);
//字符串读入有空格的字符

tolower(str[i])//转换成小写字母


string a;
  for(auto i:a)
  {
    if(i=='a'||i=='e'||i=='i'||i=='o'||i=='u')
    {
      cout<<(char)toupper(i);
    }
    else cout<<i;
  }
//字符串遇到原音字母换大写


int year = stoi(date.substr(0, 4));
        int month = stoi(date.substr(5, 2));
        int day = stoi(date.substr(8, 2));
//stoi字符串转换成数字类型
//date.substr(0,4)字符串从0开始走4位复制substr复制
