string a
getline(cin,a);
//字符串读入有空格的字符

tolower(str[i])//转换成小写字母


string a;
  for(auto i:a)
  {
    if(i=='a'||i=='e'||i=='i'||i=='o'||i=='u')
    {
      cout<<(char)toupper(i);
    }
    else cout<<i;
  }
//字符串遇到原音字母换大写


int year = stoi(date.substr(0, 4));
        int month = stoi(date.substr(5, 2));
        int day = stoi(date.substr(8, 2));
//stoi字符串转换成数字类型
//date.substr(0,4)字符串从1开始走4位复制substr复制
//date.substr(5, 2)是从第六个开始的

//提取单词
class Solution {
public:
    int countSegments(string s) {
        istringstream is(s);
        int n=0;
        while(is>>s)
        {
            n++;
        }
        return n;
    }
};



#include <ctype.h>
//判断是否为字母或数字（去除特殊符号）
isalnum()
//判断是否是字母
isalpha()
//是否为数字
isdigit()
